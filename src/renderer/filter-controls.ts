import type { Filter } from '@shared/filter'

// Stub, filled in Task 11. Until then the toolbar reads as an empty filter.
export function currentFilter(): Filter {
  return {}
}

export function wireFilterControls(_refresh: () => void): void {
  // Task 11 binds the toolbar inputs here.
}
