node_modules/
dist/
out/
.vite/
docs/
*.log
*.jsonl
# ...but synthetic test fixtures (placeholder identifiers only) are tracked.
!tests/fixtures/*.jsonl
# Local real captures - never tracked (real identifiers). Synthetic fixtures stay in tests/fixtures/.
fixtures/local/
screenshots/
release/

